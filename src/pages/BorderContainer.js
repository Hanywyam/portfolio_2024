import React, { useEffect, useState } from "react";
import Border from "./Border";

const BorderContainer = () => {
  
  return <Border scrollWidth={scrollWidth} />;
};

export default BorderContainer;
